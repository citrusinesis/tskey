export {
  clearEncryptedSeed,
  getEncryptedSeed,
  getSettings,
  hasEncryptedSeed,
  setEncryptedSeed,
  setSettings,
} from './store';
export type { StorageData, StorageSettings } from './types';
export { DEFAULT_SETTINGS } from './types';
